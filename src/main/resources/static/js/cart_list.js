function handleQuantityChange(selectElement, productId) {
    var selectedQuantity = selectElement.value;
    console.log("Selected Quantity: " + selectedQuantity + " ProductId: " + productId);

    // Call a function to update the cart item quantity using an AJAX request or other mechanism
//    updateCartItemQuantity(productId, selectedQuantity);
}
